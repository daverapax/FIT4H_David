<h1>test</h1>

<%= Date.current %>

<p>There are <%= @projects.count %> Projects listed</p>

<p><%= link_to "Neues Projekt anlegen", new_project_path %></p>

<table>
	<tr>
		<th>Name</th>
		<th>Beschreibung</th>
		<th>Zielfinanzierung</th>
		<th>Endet in</th>
		<th>Website</th>
		<th>Show</th>
		<th>Edit</th>
		<th>Delete</th>
	</tr>



<% @projects.each do |p| %>

	<tr>
	<td><%= p.name %></td>
	<td><%= truncate(p.description, length: 20) %></td>
	<td><%= number_to_currency(p.target_pledge, unit: "€") %></td>
	<td><%= done(p.ttl) %></td>
	<td><%= p.website %></td>
	<td><%= link_to "Show", project_path(p.id) %></td>
	<td><%= link_to "Edit", edit_project_path(p.id) %></td>
	<td><%= link_to "Delete", project_path(p.id), method: :delete, data: {confirm: "Sicher???"} %></td>
	</tr>
<% end %>

</table>